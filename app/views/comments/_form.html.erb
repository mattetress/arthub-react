<%= display_errors(@comment) %>

<%= bootstrap_form_for([@event, @comment]) do |f| %>
  <%= f.text_area :body, :skip_label => true %>
  <%= f.submit "Create Comment", id: "create-comment" %>
<% end %>
