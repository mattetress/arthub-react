<%= display_errors(@user) %>

<%= bootstrap_form_for @user do |f| %>

  <%= f.file_field :avatar unless @user.avatar.attached? %>

  <%= f.email_field :email %>

  <%= f.text_field :name %>

  <%= f.text_area :bio %>

  <%= f.text_field :website %>

  <%= f.submit "Save" %>
<% end %>
